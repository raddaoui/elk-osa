input:
  period: 10
  procs: [".*"]

  stats:
    system: true
    proc: true
    filesystem: true

output:
  logstash:
    hosts: ["{{ elk_server_private_ip }}:{{ logstash_beat_input_port }}"]


shipper:

logging:
  files:
    rotateeverybytes: 10485760 # = 10MB
